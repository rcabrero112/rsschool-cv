function show_about() {
    var target = document.getElementById("about");
    var rng, sel;
    rng = document.createRange();
    rng.selectNode(target)
    sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(rng);
 }

 function show_skills() {
    var target = document.getElementById("skills");
    var rng, sel;
    rng = document.createRange();
    rng.selectNode(target)
    sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(rng);
 }

 function show_code() {
    var target = document.getElementById("code");
    var rng, sel;
    rng = document.createRange();
    rng.selectNode(target)
    sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(rng);
 }

 function show_education() {
    var target = document.getElementById("education");
    var rng, sel;
    rng = document.createRange();
    rng.selectNode(target)
    sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(rng);
 }

 function show_languages() {
    var target = document.getElementById("languages");
    var rng, sel;
    rng = document.createRange();
    rng.selectNode(target)
    sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(rng);
 }